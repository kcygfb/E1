

## Codely Structured Memories

### User
- [2026-08-01 14:11:36] [2026-08-01] User is a complete Animation beginner. Teaching them Unity Animation system (Animation Clip, Animator Controller, Animation Event) to replace DOTween code-driven attack animations. Created detailed guide at Docs/Animation教学指南.md. Progress: added Animator component to PlayerPortrait, created PlayerMeleeAttack.anim, started recording keyframes. Key values: PlayerPortrait anchoredPosition=(846.76,-173.99), localScale=(5.4235,5.8491,0.9115), sizeDelta=(59.26,68.84), sprite=角色.png(535×947). Melee attack sprite=斩击.png(893×1136), attackScale=(0.4643,0.4643,1) with multiplier 1.3, hitstopScale=(0.534,0.534,1). Dash target=(826.24,-173.99) with dashOffsetX=-320. Animator controller at Assets/Animations/Player/PlayerPortrait.controller.

### Feedback
- [2026-07-22 20:22:53] User had issues with CardInteraction GlowBorder alignment — the root cause was sprite pivot offset and GlowBorder being a sibling vs child object. Final solution: GlowBorder as sibling + LateUpdate position sync. This took many iterations; if touching CardInteraction again, be very careful with GlowBorder positioning logic.
- [2026-07-24 00:26:30] Optimization lessons: (1) NEVER delete scene objects during code cleanup — git reset restores code but scene .unity may have stale references. (2) git checkout/restore of .unity file requires Unity scene reload to take effect — Unity caches scene in memory. (3) BGM DecompressOnLoad + preloadAudioData=False causes main thread block on first PlayMode after Library cache clear — must set preloadAudioData=True in .meta. (4) NEVER push without explicit user request. **Why:** multiple optimization attempts broke things, git resets caused scene reference loss and stale Unity cache. **How to apply:** only change .meta files for audio import; force reimport after git reset; always reload scene after git checkout of .unity files.
- [2026-07-28 21:37:23] Unity 6 Shader Graph Canvas target: in Universal target, set Material dropdown to "Canvas" to generate UGUI-compatible shader (has _MainTex, works with ScreenSpaceOverlay). Must Ctrl+S in Shader Graph editor after changing to recompile. **Why:** URP Universal target default material generates shaders incompatible with UGUI Canvas rendering. **How to apply:** any Shader Graph meant for UGUI Image must use Material=Canvas under Universal target.
- [2026-08-01 00:39:27] - [2026-08-01 00:40:00] CardDataV2 JSON files (melee/ranged/magic/defense.json) must NOT have UTF-8 BOM. Maomao's editor adds BOM (EF BB BF) which breaks CardJsonRepository parser with "Unexpected JSON token. Position: 0". **Why:** git merge from maomao introduced BOM on all 4 card JSON files. **How to apply:** after git pull/merge, check `Get-ChildItem Assets\StreamingAssets\CardDataV2\*.json | ForEach-Object { ([System.IO.File]::ReadAllBytes($_.FullName)[0] -eq 0xEF) }` and strip BOM with `[System.IO.File]::WriteAllBytes($path, $bytes[3..($bytes.Length-1)])` if found.
- [2026-08-01 14:11:36] [2026-08-01] AssetImportWorker crash root cause: Packages/cn.tuanjie.codely.bridge junction went missing (git doesn't track junctions). All 8 AssetImportWorker processes crashed at RegisterAssemblyDefinition reading Cn.Tuanjie.Codely.Editor.asmdef. Fix: `New-Item -ItemType Junction -Path "Packages\cn.tuanjie.codely.bridge" -Target ".codely.packages\cn.tuanjie.codely.bridge@1.0.74"`. Unity Bridge disconnects after crash, requires editor restart.
- [2026-08-03 02:52:51] - [2026-08-03 03:00:00] CardSkew + PlayUpgradeFlip bug: CardSkew (BaseMeshEffect) modifies vertex positions on the root Image, but CardArt is a child Image with its own mesh. During Y-axis flip (DOLocalRotate), CardSkew was only setting skew=0 without disabling the component — the BaseMeshEffect still ran and skewed the root Image's white background while CardArt sprite was unaffected. Fix: save original skew value, set skew.enabled=false before flip, restore skew.enabled=true + skew.Skew=originalSkew after flip completes. **Why:** BaseMeshEffect runs every frame regardless of skew value if component is enabled. **How to apply:** any time CardView.PlayUpgradeFlip touches CardSkew, must disable the component, not just zero the value.
- [2026-08-04 00:02:36] [2026-08-04] Git LFS push issue: `git push` only pushes git objects, NOT LFS objects. Teammates pulling got LFS pointer files (text) instead of actual binary content (png/mp3/wav). Must run `git lfs push --all origin master` after `git push` to push LFS objects. **Why:** .gitattributes routes all png/mp3/wav to LFS, but `git push` doesn't auto-push LFS objects. **How to apply:** after every push with new/changed binary assets (sprites, audio, textures), run `git -c http.proxy= -c https.proxy= lfs push origin master --all`. If teammate reports missing textures/sprites/audio, check `git lfs ls-files` + `git lfs push --dry-run origin master` to see if objects need pushing.
- [2026-08-05 21:44:03] [2026-08-05] TrayGridUI OnEnable bug: ShowDrag() calls gameObject.SetActive(true) which triggers OnEnable() → ShowSelection(), clearing selected materials and resetting to selection mode. Fix: use _pendingMode flag + _firstEnable guard so OnEnable only calls ShowSelection on first enable; subsequent SetActive(true) deferred to correct mode via pending flag. **Why:** OnEnable fires synchronously on SetActive(true), running ShowSelection before ShowDrag's code completes. **How to apply:** any MonoBehaviour with both SetActive and mode-switching methods must guard OnEnable against re-entrancy.
- [2026-08-05 22:47:01] [2026-08-05] UI raycast blocking: DialogueClickArea (alpha=0.01, raycastTarget=true, Canvas siblingIndex=4) and Enviroenment block raycasts to Coffeemake children (CupStack/Portafilter). Fix: move parent (BarArea) to last siblingIndex in Canvas so it renders on top. **Why:** Unity UI raycast hits topmost element; BarArea was siblingIndex=3, DCA was 4, so DCA intercepted all clicks in its area. **How to apply:** if UI drag/click doesn't reach a deeply nested element, check siblingIndex ordering against full-screen transparent panels (DialogueClickArea, Enviroenment).
- [2026-08-08 11:46:38] Sprite import mode change (Multiple→Single) breaks all scene references — Unity loses object references because sub-sprite GUIDs change. **Why:** user changed all ingredient/menu PNGs from Multiple to Single sprite mode, causing 28 null MaterialSpriteCache fields + 5 menuPages refs. **How to apply:** after any sprite import mode change, re-assign all sprite references via C# script (SerializedObject.FindProperty + AssetDatabase.LoadAssetAtPath<Sprite>). For price/name parsing from sprites, use sprite.texture.name (file name) NOT sprite.name (which becomes "X_0" in Multiple mode).
- [2026-08-10 17:49:12] [2026-08-10] PortraitExpressionCache was on Cafe Canvas GO — DontDestroyOnLoad(gameObject) persisted the ENTIRE Cafe Canvas into Event scene, causing EventSceneController to bind to wrong DialoguePanel. Fix: moved PortraitExpressionCache to its own root GO. Also added defensive Canvas lookup in ResolveSceneReferences (check canvas.scene == activeScene). **Why:** DontDestroyOnLoad on a component attached to Canvas drags the whole Canvas across scenes. **How to apply:** NEVER put DontDestroyOnLoad singletons on Canvas or any large container GO — always isolate on their own root GO. When doing GameObject.Find("Canvas") in scenes that may have persisted DDOL canvases, verify canvas.scene == SceneManager.GetActiveScene().
- [2026-08-10 17:49:17] [2026-08-10] Unity 6 LegacyRuntime font: mainTexture can be null/empty even though font != null → Text renders nothing (invisible). Was the cause of TrayGrid Count numbers not showing. Fix: re-assign font via script (txt.font = Resources.GetBuiltinResource<Font>("LegacyRuntime.ttf")) + set horizontalOverflow=Overflow, verticalOverflow=Overflow, expanded sizeDelta. **Why:** font texture lazily initialized, stale scene refs may point to broken font state. **How to apply:** if UI.Text with LegacyRuntime renders blank, check material.mainTexture — if empty, re-assign the font at runtime/editor. Arial.ttf doesn't exist in Unity 6, must use LegacyRuntime.ttf.
- [2026-08-10 17:49:21] [2026-08-10] CraftController.ResetCraft() called kettleSlot.Clear() which nulls KettleSlot.Current while the Kettle GameObject stays parented to the slot → PourOverMachine.CanStart() returns false (IsFilled=false) → button disabled until player re-drags kettle. Fix: after clearing, re-accept the kettle if still physically in slot (slot.Clear() + slot.Accept(kettleInSlot)). Also KettleSlot.Accept() was missing kettle.SetSlot(this) → kettle._currentSlot stale on re-place. **Why:** Clear() nuked the reference but left the object visually in place, creating a phantom state. **How to apply:** any Reset/Clear that nulls a container reference must rebind objects that remain physically present.

### Project
- [2026-07-22 20:22:39] Unity project E1 (github.com/kcygfb/E1.git) — a card battle + coffee shop game. Three feature branches: feature/Card-Battle (combat), feature/CoffeeShop (cafe sim), dev/maomao (teammate's branch). Master now merges all three as of 2026-07-21.
- [2026-07-22 20:22:43] CoffeeShop architecture (as of 2026-07-18): 8 functional modules under Assets/Script/CoffeeShop/ — CafeFlow, Customer, Dialogue, Order, CoffeeCraft, Inventory, Unlock, Reward. Uses GameEvent (string channel + object payload) for inter-module communication. Dialogue/Coffee/Resource data loaded from JSON in StreamingAssets/.
- [2026-07-22 20:22:46] Card-Battle visual system: CardView/CardDealAnimator/CardDragBridge/BattleCardBridge/BattleEventPresenter under Assets/Script/Combat/Runtime/. Old CombatTemp system fully removed. Card prefab at Assets/Prefabs/Card_Battle.prefab. BattleController needs rulesConfig/playerDefinition/enemyDefinitions/cardDatabase configured in Inspector.
- [2026-08-01 00:39:19] Scene Card.unity has TWO BattleController components: root "BattleController" (the real one, initializes correctly) and "Canvas/PanelSeparator/BattleRoot/HandCardPanel" (duplicate, never initializes). When wiring BattleController references in Inspector, always target the root "BattleController" object. In runtime code (Start/Awake), do NOT use FindFirstObjectByType<BattleController>() — it finds the wrong duplicate; use GameObject.Find("BattleController").GetComponent<BattleController>() instead. UPDATE 2026-08-01: CardEnemyAI must also be on root BattleController, NOT on HandCardPanel. Enemy card system (CardEnemyAI) initially placed on HandCardPanel caused enemy turn to do nothing — moved to root BC fixed it.


- [2026-07-23 22:52:14] Battle VFX system (feature/Battle-VFX branch): VFX scripts under Assets/Script/Combat/Runtime/VFX/ — PlayerAttackFeedback (melee state machine: Dashing→Slashing→Returning→Idle, with hitstop/sprite swap/screen shake/radial impact pulse/multi-layer glow slash/DamagePopup/SFX; ranged state machine: Shooting→Idle with muzzle flash/gunshot SFX; PlayRangedSingleShot for multi-shot VFX; pose state machine: SwitchToRangedPose/SwitchToMagicPose/SwitchToMeleePose on hover), EnemyHitFeedbackNew (called by PlayerAttackFeedback.PlayHit(), has squash&stretch + white→red flash), BattleVFXManager (event-driven particle spawner on VFXRoot), SlashVFX (color gradient white→red, direction/flip, multi-layer bloom glow). Magic hand: GameObject is "Magichand" at Canvas/PlayerArea/Magichand (NOT "PlayerPanel" — ConfigureMagicHandUpgradeBridge finds "Magichand" first, falls back to "PlayerPanel"). MagicHandUpgradeBridge has IPointerEnter/ExitHandler for magic pose. MagicHandInteraction (Assets/Script/UI/, namespace KiKs.UI) is independent from CardInteraction — handles hover scale/lift + glow as sibling node (SetAsFirstSibling) + LateUpdate sync. CardInteraction.cs is UNCHANGED from original (do not modify for magic hand). Magichand Image alpha was 0.6, set to 1.0. Multi-shot gun system: CombatEngine.PlaySingleShot/PlayRemainingShots/CancelShooting/IsShooting, CardDealAnimator.OnCardShot event, CardView shows [remaining/total] counter (>=1 shots shown), click=one shot at a time, drag=full play all at once, EndTurn cancels ongoing shooting. Bug fixes: ResolveAttackType falls back to card ID prefix; SwapSprite takes scaleMultiplier param + resets sizeDelta to natural size (originScale non-uniform 5.42/5.85). Inspector fields: meleeSpriteScaleMultiplier, rangedSpriteScaleMultiplier, magicSpriteScaleMultiplier, meleeSpriteOffset, rangedSpriteOffset, magicSpriteOffset. BattleBGM = いらない サカナクション REMIX on BattleBGM GameObject (loop, volume 0.5). SFX: Assets/Audio/Combat/slash_clean.wav + dash.wav + gunshot.wav. VFXRoot child of Canvas (last sibling). Canvas ScreenSpaceOverlay → uGUI Image-based VFX only.
- [2026-08-04 00:14:06] FlashScreen (扩散白光特效): as of 2026-08-04 unconditional on ALL attacks (5 call sites in PlayerAttackFeedback.cs). Was previously `if (isUpgraded) FlashScreen()` / `if (_currentAttackUpgraded) FlashScreen()`. Fix for red slash being hidden by white flash: FlashScreen creates its ImpactFlash GameObject with `SetAsFirstSibling()` on the Canvas so it renders at the bottom layer — VFXRoot (last child of Canvas) and its children (slash, muzzle flash, etc.) render on top.


- [2026-07-24 17:46:08] KiKs.Combat and KiKs.UI are in separate asmdef scopes — cannot directly reference KiKs.UI.Draggable from KiKs.Combat code. Use System.Type.GetType("KiKs.UI.Draggable, Assembly-CSharp") + reflection to access cross-asmdef static members.
- [2026-07-24 18:58:12] trajan SDF font asset has _FaceColor set to orange (1, 0.277, 0, 1) — any TMP_Text using trajan font appears orange/red regardless of Inspector color. Switched DrawPileText/DiscardPileText to DROIDSERIF-BOLDITALIC SDF (white faceColor) to fix. If other texts appear unexpectedly colored, check font material _FaceColor.
- [2026-07-28 16:09:46] Maomao pushes directly to origin/master without PRs (e.g. commit 66e4331 "7.28 主界面初步完成"). When checking for his latest work, `git fetch origin` and diff against local master — do not look for feature branches.
- [2026-07-31 22:22:29] BattleBGMController.cs was missing from master (GUID 9745ae9a8e3405145977976c3e7beda3). On 2026-07-31: created new BattleBGMController.cs (Assets/Script/Combat/Runtime/, namespace KiKs.Combat) — plays AudioSource on BattleStarted event, stops on Victory/Defeat. Removed missing script from BattleBGM GameObject in Card.unity, added BattleBGMController + wired battleController reference. Now committed and pushed to master.

- [2026-08-02 01:38:44] TransitionEffect (KiKs.UI) dual-panel architecture, now in ALL 3 scenes (MainMenu, Cafe, PreBattle). Teans2 has ExitPanel + EntrancePanel. CenterImage under ExitPanel. Flow: ExitPanel.PlayExit (alpha 0→1, centerImage, slider 0→2) → load scene → Time.timeScale=0 → EntrancePanel.PlayEntrance (slider 0→2, alpha 1→0, uses SetUpdate(true) for unscaled time) → Time.timeScale=1. KEY: entrance material belongs to DEPARTURE scene (set at Init), applied after entrance anim completes for next transition. IsEntrancePlaying static flag + WaitEntrance() static coroutine available for Start() yielding. TimeSystem.CompleteEndShopPhase calls TransitionEffect.Instance.TransitionTo(preBattleSceneName) with null fallback to SceneManager.LoadScene.








- [2026-07-29 16:05:14] UI animation/effect system (as of 2026-07-29): ButtonFeedback.cs (Assets/Script/UI/) is the universal button animation script — attach to any RectTransform for hover scale + press feedback, zero config. Optional selectionFrame + animator fields for frame animation on hover. ButtonHoverHighlight.cs is the older version tied to 选中框 specifically. 选中框.anim: 11-frame sprite sheet (12fps, play 1x, hold last frame, no loop). 选中框Controller.controller has Write Defaults disabled. CoffeeListPreviewEditor.cs (Assets/Script/Editor/) generates editor-mode preview of CoffeeListPopulator dynamic buttons via Tools > CoffeeShop > Preview/Clear Coffee List Preview. Preview items use __Preview_ prefix, placed directly under Content to use its HorizontalLayoutGroup.
- [2026-07-30 15:52:32] Coffee crafting QTE system (updated 2026-07-30): 5 QTE types under Assets/Script/CoffeeShop/QTE/ — QTEBase (BuildSpecificUI is now virtual not abstract; UI elements are [SerializeField] with null-check fallback to code generation; Complete() has result feedback: text scale bounce + panel color flash gold/green/red + 1.2s display time), RotationStopQTE (pointer=6×90 needle bar pivot bottom, Circle=200×200 Filled Radial360 Image), DropStopQTE, HoldReleaseQTE, RhythmTapQTE, RapidTapQTE. All 6 scripts have [Header("图片资源")] Sprite+Color fields (null=default pure color). Cafe scene: 5 QTE GameObjects under Canvas now have REAL scene child objects (QTE_Dim, QTE_Panel→StepTitle/ResultText/Title/[specific elements]) — saved in scene, visible in Hierarchy, editable in Inspector. EnsureUIBuilt skips creation if refs already assigned. QTEResult.cs: QTERating enum (Perfect=1.5x, Good=1.0x, Miss=0.5x). CraftController.LaunchQTE passes displayName. KEY: QTE GameObjects start inactive — Awake() doesn't fire, EnsureUIBuilt() lazy-init in Show() still works as fallback.

- [2026-07-30 14:27:40] - [2026-07-30 06:27] Shader_Transition_Dissolve 1.shadergraph (user's copy): added Alpha Lerp node so Alpha = Lerp(Trans_Before.A, Trans_After.A, dissolveMask) × Trans_After_Alpha. Fixes transparent PNG _Trans_Before showing white (transparent pixels have RGB=1,1,1). Original shader (Shader_Transition_Dissolve.shadergraph) unchanged. Mat_UI_Transition 2.mat uses the new shadergraph.
- [2026-07-30 14:27:43] - [2026-07-30 06:27] QTE system sprites now replaceable via Inspector: all 6 QTE scripts (QTEBase + RotationStop/DropStop/HoldRelease/RhythmTap/RapidTap) have [Header("图片资源")] Sprite + Color fields for every visual element (panel, circle, track, bar, pointer, fill, target/perfect zones). Fields are optional — null = uses original default pure color. Also: CustomerQueue has npcMoveSpeed field (default 500f, public on Inspector under Visual Spawn header) for NPC entrance speed; set via currentNpc.moveSpeed = npcMoveSpeed in TrySpawnNextNpc.
- [2026-07-30 15:53:28] - [2026-07-30 15:00] CoffeeListPopulator now supports Prefab template: itemPrefab field (Assets/Prefabs/CoffeeItem.prefab) with Image+Button+Text(TMP)+ButtonFeedback. If assigned, instantiates from prefab and binds data at runtime (name, icon, locked state, onClick). If null, falls back to original code generation. Prefab assigned to CoffeeList GameObject's CoffeeListPopulator in Cafe scene.
- [2026-08-10 11:57:37] - [2026-08-10 updated] Cafe flow: DailyRevenueSummary triggers on PhaseChanged(Settlement) — Cafe scene skips popup and calls TimeSystem.NotifySettlementComplete directly. Btn_EndDay calls TimeSystem.EndShopPhase which routes to EnterEndOfDay phase.

- [2026-07-30 22:17:35] - [2026-07-30 21:00] NPC dialogue system: speaker field in JSON now takes priority over speakerOverride. DialoguePlayer.ShowLine() changed from `speakerOverride ?? line.speaker` to `line.speaker ?? speakerOverride`. This fixes all dialogue showing NPC name instead of actual speaker (艾薇儿 vs NPC). NPC "Barddy" renamed to "神秘快递员" — npcName changed in NPCData asset, speaker updated in all 6 barddy_*.json dialogue files. Dialogue IDs unchanged (still barddy_arrival etc). 前辈小提 NPC added: npcId=2, spawnOrder=First, desiredCoffeeId=PourOver, portrait=Assets/Art/NPC/前辈小提.png, dialogues=timi_arrival.json (12 lines) + timi_departure.json (2 lines). Day 1 override: 前辈小提→linshuil→神秘快递员 (3 NPCs, maomao removed from Day 1). LineText horizontalOverflow set to Wrap for auto line wrapping.
- [2026-07-31 14:22:46] - [2026-07-31 14:10:00] Project E1 uses Input System package (NOT legacy Input). Input.GetKeyDown(KeyCode.X) throws InvalidOperationException at runtime. Must use UnityEngine.InputSystem.Keyboard.current[Key.X].wasPressedThisFrame instead. **Why:** Player Settings active input handling = Input System Package only. **How to apply:** all runtime key detection in this project must use InputSystem API, not UnityEngine.Input.
- [2026-07-31 19:03:09] - [2026-07-31 19:00:00] Cafe BGM player (CafeBGMPlayer.cs, Assets/Script/UI/, namespace KiKs.UI) created and added to Cafe scene under Canvas. Walkman icon (Assets/Art/Coffee/mp3.png) as MainButton sprite. Structure: CafeBGMPlayer→MainButton + ExpandPanel→PlayPauseButton(▶/❚❚) + NextButton(⏭) + TrackName. BGM folder: Assets/Audio/Cafe/BGM/ (user adds their own audio files). Bgm Clips list in Inspector. loop=false, auto-play first track on Start(), auto-advance to next track when current finishes (Update checks !isPlaying), wraps around to first track after last. F9 debug key in TimeSystem skips to PreBattle with transition effect.
- [2026-07-31 19:03:14] - [2026-07-31 19:00:00] Card scene HandCardArea structure: GlowBorder objects are siblings (not children) of card GameObjects, alternating [GlowBorder, Card, GlowBorder, Card, ...]. All GlowBorder alpha=0 at idle (shown on hover via CardInteraction). No duplicate glows — white border effect comes from card prefab's CardArt layer, not GlowBorder. PlayerArea also has a GlowBorder (blue, alpha=0) for Magichand.
- [2026-07-31 22:22:46] - [2026-07-31 19:30:00] Build configuration (2026-07-31): Product Name = "KillKissv0", Company = "EncoreDance". Build Settings scenes: [0] MainMenu, [1] Cafe, [2] PreBattle, [3] Card, [4] Collect. Window: 1920×1080 fixed non-resizable windowed mode. All CanvasScalers set to ScaleWithScreenSize 1920×1080 matchWidthOrHeight=0.5. First build succeeded at 345MB to Desktop\KillKissv0_Build. Player Settings use Input System Package only, Mono2x backend, URP.
- [2026-07-31 22:22:52] - [2026-07-31 19:30:00] Card_Battle.prefab updated: added CardArt (Image, preserveAspect), FlashLayer (Image alpha=0), CardInteraction, Draggable, CardSkew as permanent children/components. Card_Editable.prefab was created then deleted — was a mistake (not referenced by game). CardView/CardInteraction skip runtime creation when children already exist. CardDealAnimator.cardPrefab = Assets/Prefabs/Card_Battle.prefab (the only prefab used at runtime for dealing cards).
- [2026-08-01 00:22:32] - [2026-08-01 00:30:00] Enemy card system framework built (6 steps complete): (1) CombatantDefinition gains enemyCardIds/cardsPerTurn/enemyHandLimit/enemyActionsPerTurn/baseActionPoints/fallbackDamage fields. (2) BattleState gains _enemyDecks dict + _enemyBaseActionPoints dict + GetEnemyDeck/RegisterEnemyDeck/RegisterEnemyBaseActionPoints/GetEnemyBaseActionPoints. (3) BattleController.CreateEnemyDecks creates DeckState per enemy with HasEnemyDeck, instanceId prefix "enemyId_", separate random seed. (4) CombatEngine gains DrawEnemyCards/PlayEnemyCard/DiscardEnemyHand + ResolveEnemyCardEffects/ResolveEnemyDamage/ResolveEnemyToughnessDamage/ResolveEnemyLifeSteal/ResolveEnemyLifeStealMaxHealth — enemy AP restored at turn start, spent on card play. (5) EnemyAIStrategy abstract ScriptableObject (Assets/Script/Combat/AI/) + SimpleCardAI default impl (draw→sort by damage→play while AP allows→fallback). (6) CardEnemyAI MonoBehaviour (Assets/Script/Combat/Runtime/) replaces SimpleEnemyAI, listens EnemyTurnStarted, runs strategy coroutine. BattleController gains DrawEnemyCards/PlayEnemyCard/DiscardEnemyHand/GetEnemyDeck/GetEngineInternal forwarding. Step 7 pending: replace SimpleEnemyAI in Card.unity scene with CardEnemyAI.
- [2026-08-01 01:13:20] - [2026-08-01 01:00:00] EnemyCardPresenter.cs (Assets/Script/Combat/Runtime/) created — listens to CombatEvent, creates/animates enemy card visuals. CardDrawn→spawn card from deck area to hand area, CardPlayed→fly to player area, CardDiscarded→fly to discard area, PhaseChanged to PlayerTurnStart→clear all. Uses Card_Battle.prefab at 0.6 scale, interaction disabled. Scene has EnemyHandArea (top center), EnemyDeckArea (top right), EnemyDiscardArea (top left) under Canvas. SimpleCardAI timing slowed to drawDelay=1.0s, playDelay=1.5s, fallbackDelay=1.0s. FindEnemyDefinitionById added to BattleController for AI to read CombatantDefinition config. Enemy_Minion.asset configured with player card IDs as test data (not separate enemy cards yet — user may want dedicated enemy cards later).
- [2026-08-02 01:23:38] Per-scene transition materials (all use alpha-fixed shader 7b559cc85111b9e40a6c4ae9421db8cf): MainMenu exit=Main1.mat(GUID 0ce41c) entrance=Main2.mat(GUID 11ba82); Cafe exit=Cafe1.mat(GUID 816a6e) entrance=Cafe2.mat(GUID 52894d); PreBattle exit=pre1.mat(GUID 3cbf16) entrance=pre2.mat(GUID f578b9). Naming: *1=exit, *2=entrance. All _Trans_After_Alpha=0 at idle. Old materials (Mat_UI_Transition.mat, Mat_UI_Transition 1.mat, Mat_Main_Out.mat, Mat_In.mat, Trans2.mat) are orphan/unused.



- [2026-08-01 16:44:48] [2026-08-01] PlayerAttackFeedback Animator migration in progress: Melee attack fully working (DOTween dash → Animator trigger). Ranged attack partially working (clip recorded with scale 0.375/0.345/0.375 for recoil, events OnRangedShootFrame + OnAttackAnimationEnd). Key issue resolved: OnAttackAnimationEnd must NOT blindly RestoreSprite — it checks _isRangedPose/_isMagicPose and keeps the appropriate pose (because mouse may still hover on card after attack). Animator must be disabled BEFORE RestoreSprite to prevent overwriting. On attack start, animator.Play("Idle",0,0) + ResetTrigger before SetTrigger to avoid stuck state. Magic attack clip not yet created. Transition conditions: AttackType Equals 0/1/2 + Attack Trigger.
- [2026-08-01 17:50:33] [2026-08-01] PlayerAttackFeedback Animator migration update: Full Animator system now controls sprite/scale/pose via Pose parameter (0=melee, 1=ranged, 2=magic). Hover pose switching done via animator.SetInteger("Pose", X). Animation Events: OnMeleeDashStart (0.017s, moved from 0.000 due to Unity bug where 0-frame events don't fire), OnMeleeHitFrame, OnRangedShootFrame, OnAttackAnimationEnd. Write Defaults disabled on ALL Animator states (was True, caused Animator to overwrite DOTween position every frame). DOTween position tweens use SetUpdate(UpdateType.Late) to execute after Animator evaluation. Melee clip must NOT have AnchoredPosition/LocalPosition bindings (position controlled by code). Melee clip currently missing Sprite track and has wrong Scale values (original 5.42 instead of attackSprite 0.4643) — needs re-recording. SlashVFX OnComplete needs null check (this != null) before Destroy to prevent DOTween safe mode errors.
- [2026-08-02 00:04:28] [2026-08-01] Known bug: CombatEngine.PlayCard enters AwaitingExecutionConfirmation phase when toughness is broken but enemy isn't killed. ConfirmExecution() exists but is never called by any UI/code — player gets stuck unable to play cards or end turn. User is aware but chose to leave it unfixed for now (will add execution confirmation UI later). **Why:** no UI button or event listener calls BattleController.ConfirmExecution(). **How to apply:** if user reports "打牌报错" or cards stuck, this is the cause.
- [2026-08-02 01:00:57] [2026-08-02] Card upgrade flip animation: CardView.PlayUpgradeFlip() does Y-axis 0→90° (0.18s) → swap sprite → 90°→0° (0.18s) with slight scale bounce. Disables CardInteraction during flip to prevent hover interference. MagicHandUpgradeBridge calls PlayUpgradeFlip instead of SetUpgraded(true); magic fire spawns before flip starts (user preference).
- [2026-08-02 01:38:28] [2026-08-02] TransitionEffect per-scene material mechanism: DontDestroyOnLoad singleton keeps its own panels. On new scene load, Awake calls CaptureSourceMaterials (stores Material asset refs only, does NOT touch singleton panels). KEY RULE: entrance material belongs to the DEPARTURE scene (plays during arrival at new scene), NOT the arrival scene. New scene's entrance material is applied AFTER entrance animation completes, for the NEXT transition. Exit material stored in _pendingExitSourceMat, applied at next TransitionTo/Play. CopyMaterialFrom creates new Material(srcMat) + swaps Image.material but disables Image first to prevent flash. Per-scene materials: Main1/Main2, Cafe1/Cafe2, pre1/pre2 (all alpha-fixed shader 7b55). Old AdoptPanels approach (reparent/destroy panels) caused pink flash — abandoned.
- [2026-08-03 02:52:44] - [2026-08-03 02:00:00] CardFire shader (Assets/Shaders/CardFire/CardFire.shader) for upgraded card visual effect. Must use CG (UnityCG.cginc) NOT URP HLSL — ScreenSpaceOverlay Canvas doesn't render URP HLSL shaders. _MainTex_TexelSize unreliable on Overlay Canvas so edge detection doesn't work. Attempted multiple approaches (alpha edge detection with _EdgeWidth, UV border detection, FireGlow layer with whiteTexture sprite, FireGlow with card sprite) — ALL FAILED on ScreenSpaceOverlay. CardView.ApplyUpgradeMaterial() and FireGlow layer code have been REMOVED. CardView.cs reverted to pre-fire state: SetUpgraded only does RefreshCardName+RefreshCardArt, PlayUpgradeFlip only does flip animation. CardFire.shader and CardFire.mat files still exist but are unreferenced. To make fire outline work, need ScreenSpaceCamera Canvas mode (too invasive) or a different approach entirely (e.g. additional Image with simple border shader, or particle system). User was frustrated after multiple failed attempts — do not attempt again without a fundamentally different approach.
- [2026-08-04 00:02:29] [2026-08-04] PlayerPortrait Animator 姿态 clip 实际在 `Assets/Art/战斗场景/Animi/` 目录下（PlayerMagicPose.anim / PlayerRangedPose.anim / PlayerRangeAttack.anim），不在 `Assets/Animations/Player/`。之前误判为"缺失"是搜索范围不对。PlayerMagicPose.anim 原本只有 sprite 曲线无 sizeDelta/position 曲线，从枪械 pose 切魔法 pose 时 sizeDelta/pos 残留枪械值导致立绘拉伸变形（用户描述为"缩放"）。Fix: 给 PlayerMagicPose.anim 补了 sizeDelta=(42.93,65.65) + pos=(-84.91,-179.40) 曲线（取自 PlayerMagicAttack.anim 起始值），不碰 scale。
- [2026-08-09 10:39:57] [2026-08-09] Coffee battle item system — cross-scene crafting integration: (1) CoffeeDataJson gained battleEffect field (BattleEffectJson: type/amount/target). (2) CoffeeEffectRegistry (KiKs.Combat) changed from hardcoded to dynamic — reads JSON files directly from StreamingAssets/CoffeeData (cannot reference CoffeeDataLoader due to asmdef boundary). APIs: Load()/Reload()/TryGet()/GetDisplayName()/HasEffect(). (3) RuntimeGameRepository gained CraftedCoffeeIds (HashSet, deduped) + AddCraftedCoffee/ClearCraftedCoffees. (4) OrderSystem.TryServeCoffee calls AddCraftedCoffee(coffeeId) on successful serve. (5) TimeSystem.StartShopPhase clears CraftedCoffeeIds each day. (6) CoffeeSelectionUI reads from RuntimeGameRepository.CraftedCoffeeIds (only shows coffees with battleEffect via HasEffect), falls back to PourOver/BloodGarment if empty. Deleted pourOverIcon/bloodGarmentIcon Inspector fields — icons now via reflection to CoffeeIconCache.Instance.GetCoffeeSprite (asmdef boundary). (7) CoffeeSlotController also uses CoffeeIconCache reflection. Flow: Cafe craft→OrderSystem records→PreBattle shows crafted coffees→Card uses them.




- [2026-08-09 10:39:49] [2026-08-09] CoffeeEffectRegistry 动态加载已实现：从 StreamingAssets/CoffeeData/*.json 的 battleEffect 字段读取（自读文件，不依赖 CoffeeDataLoader，因 KiKs.Combat asmdef 不能引用 Assembly-CSharp）。3款咖啡有 battleEffect: PourOver(Heal 25/Self)、Espresso(Damage 15/Enemy)、Americano(Bleed 3/Enemy)。其余咖啡暂无。


- [2026-08-04 09:15:29] - [2026-08-04 10:30:00] [2026-08-04] Remote commit 9d709b7 "8.4 战斗系统重构" pulled via git stash → pull → stash pop. Major changes: CombatFlowController.cs (new, 679 lines) extracted from CombatEngine, CombatEngine heavily refactored (1079 lines removed/replaced), enemy card JSONs added (enemy_big_eye/enemy_dog/enemy_little_girl.json), CardJsonRepositoryTests + CombatEngineTests updated, multiple docs added. Coffee feature auto-merged with no conflicts (6 overlapping files: CombatEngine/BattleController/CardSelectionUI/Card.unity/Cafe.unity/PreBattle.unity).
- [2026-08-04 13:33:44] PlayerGlobalStats (static class, Assets/Script/Combat/Runtime/) is the cross-scene player HP system. BattleController syncs HP to it at battle start, on every DamageApplied/HealingApplied event (ForwardEvent), and at DisposeEngine. PlayerGlobalHpUI reads from it for PreBattle scene display. CoinUI + InventorySystem (gold) reused across Cafe/Card/PreBattle scenes.
- [2026-08-04 20:53:47] [2026-08-04] Coffee craft system refactored: no longer selects coffee from list. Player crafts freely (all step buttons always interactable), then on Deliver the system matches performed step ID sequence against CoffeeDataLoader JSON recipes. CraftController now lives on BarArea (not Coffeemake, which is inactive by default). OrderCreated event auto-triggers StartFreeCraft(). Wrong coffee → ResetCraft (stays in Make panel), Correct coffee → EndCraft. Reset button (Btn_Reset) added next to Deliver. StepDef hardcoded in CraftController maps stepId→(displayName, resourceId, amount, qteType).
- [2026-08-04 22:36:39] [2026-08-04] MaterialSelector (Assets/Script/CoffeeShop/CoffeeCraft/) — horizontal infinite scroll list for Grind step material selection. 3x copy infinite loop, DOTween snap, InputSystem arrow keys. Must use _isSnapping flag during ShiftContent to prevent re-entrant OnScrollChanged infinite loop. MaterialSelector lives on Canvas (not Coffeemake) as last sibling to avoid DialogueClickArea blocking. CraftController on BarArea (not Coffeemake which is inactive by default).
- [2026-08-05 11:00:21] [2026-08-05] CraftController.MatchCoffeeBySteps changed from exact match to tail match — player can do extra steps (e.g. Grind+Grind+PourOver) and still match recipe [Grind, PourOver]. Deliver button now always interactable when steps > 0, no longer requires pre-matching.
- [2026-08-08 13:21:45] [2026-08-08 updated] MenuViewer.cs (Assets/Script/CoffeeShop/) — cafe menu popup UI. Btn_OpenMenu at top-left corner, ViewerPanel centered with MenuImage + Btn_NextPage + Btn_Close (X) + TierBar(4 buttons: 100/200/300/400). Price tier filtering: 100档=1-99, 200档=100-199, 300档=200-299, 400档=300-399. MachineMenu.png (no price suffix) only in 100档 (tier 0) at end, NOT every tier. 15 menu sprites in Assets/Art/Coffee/CoffeeIcon/Menu/ (filename pattern: {Name}Menu_{Price}.png). Price parsed from sprite.texture.name (NOT sprite.name). Selected tier button highlighted with selectedColor (yellow). Btn_OpenMenu hidden when ViewerPanel open, restored on Close. All under Canvas/MenuViewer. Panel inactive by default.


- [2026-08-05 14:58:13] [2026-08-05] CraftController StepDef gained `useMaterialSelector` field — decouples MaterialSelector from Grind step. Any step with useMaterialSelector=true triggers MaterialSelector.Show(). _materialSelectStepId tracks which step requested selection, OnMaterialConfirmed resumes that step's QTE. No longer hardcoded to "Grind".
- [2026-08-07 12:48:36] [2026-08-07 updated] DialoguePlayer.AnimateSpeaker() — when a dialogue line shows, the speaker's portrait gets a DOTween scale bounce (1→1.08→1, 0.24s). Player matched by name=="艾薇儿" → Canvas/PlayerArea/PlayerP. NPC matched via CustomerController.ActiveCustomers static Dictionary (npcName→instance) with fuzzy name match (Contains both ways). Replaced old FindObjectsByType<CustomerController> scan.

- [2026-08-05 14:58:13] [2026-08-05] MenuButtonAnim (in MenuViewer.cs) — standalone component for button hover/click animation: OnPointerEnter scale 1→1.15 OutBack, OnPointerExit scale→1 OutQuad, OnPointerDown pull down Y -8px, OnPointerUp spring back OutBack. Auto-added by MenuViewer.Awake to openButton GameObject. Requires button pivot at (0.5,0.5) for centered scaling.
- [2026-08-05 15:57:32] [2026-08-05] ButtonGlow.cs (Assets/Script/UI/) — universal PNG edge glow component. Final algorithm: RenderTexture reads sprite alpha → dilation (expand outward) → box blur → subtract original alpha (edges only) → bake glowColor into texture. glowRadius is in screen pixels, auto-scaled to texture pixels via texelToScreen ratio. GlowLayer created as sibling (before button in hierarchy order) so it renders behind. [ExecuteAlways] for editor preview.
- [2026-08-08 11:15:19] [2026-08-08 updated] Coffee craft material system: MaterialDefinition.cs now has 11 raw materials (claw/wolffur/eye/fire/oil/snake/tentacle/CoffeeBean/Milk/Sugar/Water) + 26 output materials (5 generic: GroundCoffee/Espresso/SteamedMilk/PourOverCoffee/Unknown + 7 Powder + 7 Espresso + 7 Ball). ID renames on 2026-08-08: eyeball→eye, purpleFlame→fire, snakeDried→snake, wolfHair→wolffur (file-name aligned). New raw: oil (肥油). MaterialSpriteCache.cs uses [FormerlySerializedAs] on renamed fields to preserve scene refs — 48 sprite fields total, all assigned in Cafe scene. MachineRecipeLibrary has 4 generic + 21 material-specific recipes. Partial chain: raw→(Grinder)→powder→(Extractor)→espresso; powder+water→(PourOver)→ball. Sprite assets: Powder/ in Assets/Art/Coffee/CoffeeIcon/Powder/, espresso in espressoliquid/, balls in Pouroverball/. NOTE: snake file naming inconsistent — Powder=snakePowder, Espresso=snakejerkEspresso, Ball=snakejerkBall.

- [2026-08-05 23:31:29] [2026-08-05] Coffee craft Phase 2: CupStack/Portafilter/CupContainer all use transform.position = eventData.position for dragging (NOT anchoredPosition — causes offset when parent != Canvas). CupStack: drag body→snap back→spawn CupContainer at drop pos. Portafilter: 3 states (Empty→HasMaterial→Ground), IDropHandler receives materials, drag to StepDropTarget triggers logic. CupContainer: drag to Btn_Deliver=submit, drag to StepDropTarget=trigger. All 3 under Canvas/BarArea/Coffeemake. Canvas sibling order: [3]TrayGrid [4]MaterialPalette [5]DialogueClickArea [6]DialoguePanel [7]BarArea — DialoguePanel must render above TrayGrid/MaterialPalette. Portafilter emptySprite=仙女棒_0, CupStack cupSprite=拿出的杯子（没底盘）_0. IngredientTray.SetDefaults() pre-fills Milk/Sugar/Water. Start shop condition=HasAny (≥1 slot).
- [2026-08-06 18:14:28] [2026-08-06] Coffee craft system v3 rewrite (2026-08-06). Deleted: Portafilter/PourOverKettle/CraftStation/GrindStation/ExtractStation/PourOverStation/SteamMilkStation/InputSlot (8 files). New files: MachineRecipe.cs (static recipe library: Grinder+CoffeeBean→GroundCoffee, Extractor+GroundCoffee→Espresso, Steamer+Milk→SteamedMilk, PourOver+GroundCoffee→PourOverCoffee), MaterialIcon.cs (universal draggable material icon — can drag to MaterialSlot/CupContainer/Kettle/TrayCellDrop), MaterialSlot.cs (accepts any MaterialIcon, has HintLabel), KettleSlot.cs (pour-over only, accepts Kettle), Kettle.cs (draggable container, receives MaterialIcon, pour to cup transfers all), CraftMachine.cs (base: 1 MaterialSlot + button + recipe lookup → spawn MaterialIcon at outputPoint), PourOverMachine.cs (extends CraftMachine: requires KettleSlot filled, output goes into kettle not as icon), CraftController.cs (content set matching: cup.Contents HashSet vs CoffeeDataJson.requiredMaterials HashSet — SetEquals), CupContainer.cs (accepts MaterialIcon, icons stack at center as children, drag to Deliver/CupStack), CupStack.cs (spawn CupContainer, HasActiveCup limit). CoffeeData.cs + CoffeeDataLoader.cs gained requiredMaterials field (List<string>). TraySlotDrag.cs rewritten to target MaterialSlot/CupContainer/Kettle instead of old InputSlot. Scene NOT yet wired — old InputSlot/Station components are Missing Scripts, need manual replacement with CraftMachine/PourOverMachine + MaterialSlot/KettleSlot. Coffee JSON files need requiredMaterials field added. F8=skip dialogue, F9=skip shop.
- [2026-08-08 11:15:26] [2026-08-08 updated] Coffee craft v3 scene wired: 4 CraftMachine (Grinder/Extractor/Steamer on Btn_Grind/Btn_Extract/Btn_SteamMilk, PourOverMachine on Btn_Pour), 3 MaterialSlot (Slot_Grind/Slot_Extract/Slot_Steam) + 3 OutputPoint + KettleSlot + Kettle (sprite=手冲壶.png) under Coffeemake. Slot_PourMat deleted — Kettle itself acts as material slot. MaterialDefinition now has 37 entries (11 raw + 26 output incl 21 material-specific: 7 Powder + 7 Espresso + 7 Ball). MaterialPalette only shows isRaw. MaterialIcon.OnEndDrag only adds back to inventory if isRaw. MaterialSpriteCache has 48 sprite fields total (renamed fields use [FormerlySerializedAs]). Cup prefab at Assets/Prefabs/CoffeeShop/Cup.prefab. CraftMachine.ProduceOutput detects CupContainer at outputPoint via RectTransform bounds overlap. CupContainer.TryMerge: contents SetEquals recipe → swap cup Image to coffee icon (CoffeeIconCache on Canvas, 4 sprites). CanStart: any material → button active. OnStartClicked: recipe found → produce output, no recipe → produce "Unknown". ResetCraft clears all. Icon size: 108x108 (output/drag), 72x72 (cup/kettle).

- [2026-08-10 11:57:44] [2026-08-10 updated] Dialogue system: DialogueLineJson schema = {speaker, text, expression}. JSON files: barddy.json (8 dialogues incl barddy_day2_arrival/departure), timi.json (3), timi_startofday.json (1), generic.json (3). End-of-day dialogue now handled by CustomerQueue EndOfDay phase (not ShopReadyToClose). CustomerQueue.endOfDayOverrides (List<DayNPCConfig>) allows per-day end-of-day NPC lists; empty list = no end-of-day. Barddy_Day2.asset: acceptAnyCoffee=true, barddy_day2 dialogue. GenericCustomer.asset: portraitPool(npc1-4), anyCoffeeChance=0.3, random unlocked coffee ordering.




- [2026-08-07 16:45:53] - [2026-08-07] Coffee craft changes: (1) PourOver recipe changed from single-input (GroundCoffee→PourOverCoffee) to multi-input (GroundCoffee+Water→PourOverCoffee). MachineRecipeLibrary gained MultiMachineRecipe + TryGetOutputMulti(SetEquals). (2) PourOverMachine.OnStartClicked uses kettle.Contents for multi-input matching. (3) KettleSlot gained serialized initialKettle field — Kettle defaults in slot at scene load, no manual drag needed. (4) CraftMachine.ProduceOutput now detects CupContainer at outputPoint via RectTransform bounds overlap — cup at output → direct AcceptIcon, no cup → spawn draggable icon. (5) PourOverMachine similarly checks for cup near kettle. (6) CupStack gained ButtonGlow — yellow glow when no active cup, off when cup exists. (7) CraftController re-added wrong-coffee dialogue feedback (TriggerWrongCoffeeFeedback + DialogueRequested/Ended listeners for hiding/showing coffeeMakeGroup+TrayGrid).
- [2026-08-07 19:20:01] - [2026-08-07] BuffDisplayUI system: BuffDisplayUI.cs (Assets/Script/Combat/Runtime/, namespace KiKs.Combat) reads CombatantState each frame (BleedStacks/PoisonStacks/StunTurns/VulnerabilityTurns/ImmunityTurns/NullifyAttackCharges), snapshot-compares to avoid per-frame rebuild, shows/hides icon+count dynamically. 6 Sprite fields in Inspector (bleedIcon/poisonIcon/stunIcon/vulnerabilityIcon/immunityIcon/nullifyIcon). HorizontalLayoutGroup auto-arranges. BuffBar GameObjects are siblings of PlayerPortrait/EnemyPortrait under PlayerArea/EnemyArea (NOT children — Portrait Animator scale/WriteDefaults=false would distort child BuffBar). Position offset -80 below portrait. NextWordIcon on DialoguePlayer: hidden during typing, shown when typing completes, hidden on dialogue end. Scene refs assigned in both Cafe.unity and Card.unity.
- [2026-08-07 23:41:16] [2026-08-07] Enemy portrait display chain: BattleController.ApplyPrimaryEnemyPresentation() sets enemyPortraitImage.sprite but scene had overrideSprite assigned (怪物剪影(1).png = BigEye). Unity renders overrideSprite over sprite, so switching enemies didn't change the displayed portrait. Fix: added `enemyPortraitImage.overrideSprite = null;` before setting sprite. Also fixed EnemyHitFeedbackNew scene params that were wrong: knockbackDistance 2.51→60, hitColor (1,1,1,1)→(1,0.4,0.4,1), shakeVibrato 1→15. Enemy art assets: dog.png(1600×1200), 小女孩.png(1920×1358), 怪物剪影(1).png(2048×2048) in Assets/Art/Enemy/.
- [2026-08-07 23:41:20] [2026-08-07] CustomerQueue moved from GameSystem/CustomerQueue (outside Canvas, world space) to Canvas/CustomerQueue. SpawnPoint/CounterPoint/ExitPoint children converted from world coords to Canvas-local coords relative to Canvas center (960,540). Spawn=(766,11), Counter=(615,17), Exit=(765,10). Fixes position inconsistency across different screen resolutions — now under CanvasScaler 1920×1080 reference.
- [2026-08-07 23:41:25] [2026-08-07] PreBattle scene BeginButton/Sprite Animator had m_Controller={fileID:0} (empty). ButtonPlayAnimationOnEnable.cs Update() called ResetTrigger/SetTrigger on null controller → "Animator is not playing an AnimatorController" error. Fix: assigned Assets/Animations/Begin Hunting/BeginHuntingController.controller (GUID 74a2a539d00768242a1095138c23dcaf) to the Animator. Scene saved.
- [2026-08-08 13:21:53] - [2026-08-08 12:10:00] [2026-08-08] Coffee recipe half-product system: CoffeeDataJson/CoffeeData gained halfProducts field (List<HalfProductJson>: materials + displayName). CoffeeIconCache expanded from 5 to 15 coffee sprites + 5 half-product sprites (viscousDreamHalf/afterTasteHalf/sunsetHalf/tentacleLabyrinthHalf/theFifthFlavorHalf). CupContainer.TryMerge checks halfProducts: if cup contents are a proper subset of requiredMaterials AND match a halfProducts entry → swap cup sprite to half-product appearance. Half-product sprites in Assets/Art/Coffee/CoffeeIcon/halfcoffee/. Coffee sprites in Assets/Art/Coffee/CoffeeIcon/coffee/ (14 files, space in names: "Budget Brew 1.png", "Flame Latte.png", "One Snake, Two Ways.png"). Budget Brew sprite = "Budget Brew 1.png" (user switched from "Budget Brew.png"). All 19 sprites assigned in Cafe scene.
- [2026-08-08 20:04:51] - [2026-08-08 14:00:00] [2026-08-08] Coffee recipe JSON all 14 complete. (1) BudgetBrew (05, 40C): snake→Grind→snakePowder→Extract→snakeEspresso + Milk→Steam→SteamedMilk, req=["snakeEspresso","SteamedMilk"]. (2) ViscousDream (06, 50C): claw→Grind→clawPowder→PourOver+Water→clawBall + Water→half-product + Sugar→final, req=["clawBall","Water","Sugar"], half=[{["clawBall","Water"]}]. (3) FinalGaze (07, 70C): eye→Grind→eyePowder→PourOver+Water→eyeBall, req=["eyeBall"]. (4) AfterTaste (08, 80C): oil→Grind→oilPowder→PourOver+Water→oilBall + Milk→half-product + Sugar→final, req=["oilBall","Milk","Sugar"], half=[{["oilBall","Milk"]}]. (5) OneSnakeTwoWays (09, 100C): snake→Grind→snakePowder→PourOver+Water→snakeBall + snake(raw), req=["snakeBall","snake"]. (6) TentacleLabyrinth (10, 120C): CoffeeBean→Grind→GroundCoffee + tentacle→Grind→tentaclePowder→half-product + tentacle(raw) + SteamedMilk→final, req=["GroundCoffee","tentaclePowder","tentacle","SteamedMilk"], half=[{["GroundCoffee","tentaclePowder"]}]. (7) FreeWom (11, 130C): wolffur→Grind→wolffurPowder + CoffeeBean→Grind→GroundCoffee→both→Extract(multi)→wolffurEspresso + Water, req=["wolffurEspresso","Water"]. (8) Sunset (12, 180C): eye→Grind→eyePowder→Extract→eyeEspresso + SteamedMilk→half-product + Sugar→final, req=["eyeEspresso","SteamedMilk","Sugar"], half=[{["eyeEspresso","SteamedMilk"]}]. (9) FlameLatte (13, 270C): Latte base(Espresso+SteamedMilk) + fire(raw), req=["Espresso","SteamedMilk","fire"]. (10) TheFifthFlavor (14, 300C): eye→Grind→eyePowder→Extract→eyeEspresso + SteamedMilk→half-product + eye(raw) + Sugar→final, req=["eyeEspresso","SteamedMilk","eye","Sugar"], half=[{["eyeEspresso","SteamedMilk"]}]. Base 4 JSONs (00-03) updated to 20C. CraftMachine.cs now supports multi-input: tries single first, then TryGetOutputMulti. MachineRecipe wolffurEspresso changed to multi-input: ["wolffurPowder","GroundCoffee"]→wolffurEspresso. Half-product bug fixed: now destroys icons on half-product match (was only swapping sprite). ViscousDream halfProducts fixed: clawEspresso→clawBall. MochaLatte JSON exists but no menu sprite.
- [2026-08-08 20:05:36] - [2026-08-08 14:00:00] [2026-08-08] Cafe.unity merge conflict: duplicate FileID 1373624598 (one MonoBehaviour CoffeeListPopulator with orphaned m_GameObject 1373476334, one GameObject OrderArea). User chose to have teammate fix it. I reverted my edit. If touching this again: the CoffeeListPopulator block is the orphan (m_GameObject 1373476334 doesn't exist as a GameObject definition), should be deleted or given new unique FileID.
- [2026-08-08 20:46:22] - [2026-08-08] UI/feedback additions: (1) ButtonHoverSound.cs (Assets/Script/UI/) — hover + click sound on single AudioSource, Stop() before Play() to interrupt previous. Used on 开始游戏 button (hoverSound=铅笔.mp3, MainMenu scene) and Btn_NextPage (clickSound=翻书.mp3, Cafe scene). LoadAudioData() in Awake for zero-latency. (2) PlayerHpFeedback.cs scale bug fixed: was reading currentScale dynamically → if tween killed mid-punch, next hit multiplied from enlarged scale. Fix: cache _iconOriginScale in Awake, always punch from origin→origin. (3) BuffDisplayUI Count text: added Outline component (black, 1.5px) + fontSize=iconSize*0.25 + center alignment for visibility. (4) TrayGridUI gained TogglePalette(): togglePaletteBtn + coffeeMakeGroup fields. MorningCheck=direct toggle palette; Shop=open palette hides Coffeemake, close restores. Button created at Canvas/TogglePaletteButton.
- [2026-08-08 23:10:26] - [2026-08-08] Coffee craft fixes & additions: (1) CupContainer.OnEndDrag reverted to foreach results (not results[0]) — BarImage blocks raycast to NPC portrait, must iterate all hits. (2) CraftController.ResetCraft now destroys ALL stray MaterialIcons (parent is Canvas, Output_, or anything not MaterialSlot/Kettle/CupContainer). (3) MaterialIcon.OnEndDrag: no snap-back on miss (stays at drop position); drag to Btn_Reset destroys icon. (4) MenuViewer hint bounce: DOTween Sequence (22px up→8px down→14px up→rest, 2.5s interval, loops) on OrderCreated if menu not opened; stops on Open/OrderCompleted. (5) TrayGridUI.TogglePaletteButton visibility synced with ShowSelection/ShowDrag/HideAll. (6) PlayerHpFeedback scale bug: cache _iconOriginScale in Awake, always punch origin→origin (was reading currentScale dynamically → cumulative enlargement on interrupted tweens). (7) New coffee ESSymphony (15, 240C): eye→Grind→eyePowder + snake→Grind→snakePowder → both→Extract(multi)→ESMEspresso + SteamedMilk, req=["ESMEspresso","SteamedMilk"]. MaterialDefinition + MachineRecipe + CoffeeIconCache + MaterialSpriteCache + JSON all updated. Scene sprites assigned.
- [2026-08-09 00:30:09] - [2026-08-09] Recent fixes: (1) BeginHunting.anim loop fix: stopTime was 1.667 but clip length 0.833s → halved stopTime to 0.8333 for seamless loop. (2) ButtonPlayAnimationOnEnable simplified: use _animator.enabled=true/false instead of Play("Idle")/SetTrigger — Animator runs continuously with loopTime=true, no state transitions = no stutter. (3) CoffeeSelectionUI: added openPopupButton2 field for CoffeeSelectButton (1) — was unbound, second coffee select button didn't work. (4) Currency display: all "G" → "C" in DailyRevenueSummary (3 places) + HuntResultPresenter (1 place). (5) OrderUI: added ticketPriceText (TMP_Text) showing "{sellPrice} C" on order ticket. PriceText GameObject created under OrderTicketImage, assigned via script. (6) Menu sprite updates: SunsetMenu_180c.png and TheFifthFlavorMenu_300c.png replaced in menuPages array (indices 11 and 13).
- [2026-08-09 11:25:25] [2026-08-09 11:25] Portrait expression (立绘差分) system: (1) DialogueLineJson gained `expression` string field — empty/null = default portrait. (2) PortraitExpressionCache.cs (Assets/Script/UI/, namespace KiKs.UI, DontDestroyOnLoad singleton on Canvas) — Inspector-configured CharacterExpressions list: characterName + defaultSprite + List<ExpressionEntry(expression, sprite)>. GetSprite(characterName, expression) returns matching sprite or defaultSprite. (3) DialoguePlayer.SetExpression(speaker, expression) called after AnimateSpeaker in ShowLine — finds portrait Image (player=Canvas/PlayerArea/PlayerP, NPC=CustomerController.ActiveCustomers fuzzy match), swaps sprite via PortraitExpressionCache. (4) Cafe scene Canvas has PortraitExpressionCache: Avril 6 expressions (Tired=劳累, Annoyed=恼, Irritated=恼, Thinking=思考_不悦, Professional Smile=营业笑容, Happy=开心, default=普通) + Xiaoti 1 expression (Annoyed=傲娇, default=普通). Sprite paths: Avril=Assets/Art/Coffee/PlayerP/, Xiaoti=Assets/Art/Coffee/xiaotiP/.
- [2026-08-09 15:55:39] [2026-08-09] Treasure area architecture: 4 files under Assets/Script/Combat/Treasure/ — TreasureSceneController (auto-installed via [RuntimeInitializeOnLoadMethod], flow driver), TreasureDefinitions (DTOs), TreasureJsonRepository (reads StreamingAssets/Treasure/treasures.json, 4 offers fixed), TreasurePurchaseSession (temp wallet, weighted random reward). Scene: Treasure.unity (in Build), Event.unity is duplicate (not in Build). Flow: PreBattle map click → DailyAreaMapState selects treasure point → TransitionEffect → load scene → DealOffers (4 cards via CardDealAnimator, category="treasure", hidden combat text) → player clicks card → HandleOfferPlayed → TryPurchase → RevealReward (DOTween token flies to tray) → leave → back to PreBattle. **Rewards are session-only, NOT persisted to RuntimeGameRepository/InventorySystem yet.**
- [2026-08-10 14:08:43] [2026-08-10 14:00 updated] Event area (事件区) fully implemented with 3-day rotation. EventCardDefinition has: hpCost, goldCost, goldRewardMin/Max, materialRewardId ("random_raw"=随机原始材料), materialRewardAmount, cardRewardMode ("random_normal"/"random_special"/"specific"), cardRewardIds (string[], takes priority over cardRewardMode), healFull bool. Card types: effect/attack/pilfer/end. Rewards PERSIST via RuntimeGameRepository.AddGold/AddResource/AddOwnedCard + PlayerGlobalStats.SetHealth. EventSelectionState.PickEventForDay(day) reads RuntimeGameRepository.CurrentDay, filters events by e.day==day. FinishEvent now calls RuntimeGameRepository.CompleteSelectedArea(defeated:false) (same as Treasure) — auto-advances day when 3rd exploration reached. NPC portrait: Resources/Art/NPC/Event/{npcId}.png. event_dialogues.json has dialogues for all 3 days. Card ID prefix rule: all card IDs in this project have category prefixes (ranged_, heavy_, bleed_, misc_, magic_) — reward JSONs must use full prefixed ID, not short name.

- [2026-08-10 02:25:26] [2026-08-09 20:30] Defeat (战败) logic: (1) PlayerAttackFeedback.cs OnDefeat() fires Animator trigger "Defeat" (user configured Any State→PlayerDefeat with PlayerDefeat.anim single-frame 战败.png). (2) HuntResultPresenter.cs ShowAfterDefeatRoutine waits 2s, cancels map point, TransitionEffect.Instance.TransitionToWithOverride("PreBattle", defeatOverride) — does NOT touch day count, does NOT go to Cafe. Returns to PreBattle so player can retry. defeatOverride has 3 optional fields: defeatExitMaterial/defeatCenterSprite/defeatEntranceMaterial. (3) TransitionEffect.cs TransitionOverride class + TransitionToWithOverride(). CardSelectionUI gained 3 groups of override fields (battle/treasure/event) for PreBattle→each area transitions.

- [2026-08-10 01:17:49] [2026-08-09 23:50] PreBattle card selection improvements: (1) Card hover preview panel (CardPreviewPanel with PreviewImage/PreviewName/PreviewDesc) on right side — ShowCardPreview/HideCardPreview via CardItemInteraction. PreviewDesc uses CardDescriptionFormatter.Format (TMP sprite tags) + CardIcons SpriteAsset + 站酷文艺体 SDF font. (2) CardItemInteraction — custom lightweight component (IPointerClickHandler+IPointerEnterHandler+IPointerExitHandler only, NOT IDragHandler/IScrollHandler) replaces Button+EventTrigger. Button implements IDragHandler (empty) which consumes drag events → blocks ScrollRect. EventTrigger also consumes events. CardItemInteraction lets drag/scroll bubble to parent ScrollRect. Has built-in hover scale 1→1.08 via DOTween. (3) CoffeeSelectionUI: selected coffee slots no longer show name text (icon only). (4) CardSelectionUI deck slot click-to-remove (OnDeckSlotClicked) still uses Button (slots are NOT in ScrollRect).
- [2026-08-10 12:22:57] [2026-08-10 12:23 updated] Cafe NPC system refactored to identity/appearance separation. **NPCData** = identity only (npcId, npcName, speakerAliases, speakerColor, portrait, portraitPool, portraitSize). **NPCEntry** (inline [Serializable]) = one appearance: npc ref + dialogueKey + orderMode (SpecificCoffee/RandomUnlocked/AcceptAny) + coffeeId. **DayConfig** (ScriptableObject) = one day: day + startOfDay[NPCEntry] + customers[NPCEntry] + endOfDay[NPCEntry]. No DayConfig = random day (GenericCustomer × min~max). CustomerQueue.dayConfigs (List<DayConfig>) replaces all old dayOverrides/endOfDayOverrides/npcPool. StartOfDayController reads from CustomerQueue.GetDayConfig(day).startOfDay. Dialogue ID = dialogueKey + "_" + phase (arrival/departure/startofday/endofday). 6-phase pipeline: StartOfDay→MorningCheck→Shop→EndOfDay→Settlement→Night. TimeSystem sole driver with Notify* callbacks. locked/return-visit system deleted. Day1Config.asset + Day2Config.asset created. timi_startofday_2 renamed to timi_startofday in JSON.


- [2026-08-10 03:07:00] [2026-08-10 03:00] Speaker alias system: (1) NPCData gained speakerAliases List<string> field — DialoguePlayer matches speaker name against npcName OR any alias (fuzzy Contains both ways). CustomerController.MatchesSpeaker(speaker) checks npcName + all aliases. DialoguePlayer.SetExpression/AnimateSpeaker use MatchesSpeaker instead of direct string compare. Xiaoti.asset: npcName="Xiaoti", speakerAliases=["前辈小提","小提"]. (2) DialoguePlayer gained playerAliases string[] field (default ["艾薇儿"]) + IsPlayer(speaker) method — playerName stays "Avril" (matches PortraitExpressionCache characterName), playerAliases adds "艾薇儿". All 3 speaker==playerName checks replaced with IsPlayer(). Both English and Chinese dialogue speaker names now work.
- [2026-08-10 14:07:39] [2026-08-10 14:00 updated] Event area (事件区) 3-day plan NOW IMPLEMENTED. events.json has 3 events: evt_day1_watchman(day=1, npcId=watchman, 4cards), evt_day2_poleman(day=2, npcId=poleman, 3cards), evt_day3_namelessking(day=3, npcId=namelessking, 3cards). NPC portraits at Resources/Art/NPC/Event/{watchman,poleman,namelessking}.png. Two bugs fixed: (1) cardRewardIds in events.json had short names (flamethrower/greatsword/reaper/magicburst) → fixed to full prefixed IDs (ranged_flamethrower/heavy_greatsword/bleed_reaper/misc_magic_burst). (2) FinishEvent was calling DailyAreaMapState.CompleteSelectedPointWithoutCountingExploration() instead of RuntimeGameRepository.CompleteSelectedArea(defeated:false) → day never advanced when event was 3rd exploration → player stuck in PreBattle. Fixed to match Treasure pattern.
- [2026-08-10 17:49:24] [2026-08-10] MenuViewer.cs Open() now remembers last tier/page across open/close. _currentTier/_currentPage are instance fields that persist; Open() restores them via SelectTier(lastTier) then sets _currentPage=lastPage. Falls back to FindFirstAvailableTier() if last tier no longer unlocked. Removed unused _menuOpenedThisOrder field (CS0414 warning).

### Reference
- [2026-08-01 01:21:42] - [2026-08-01 01:20:00] 技术文档：2026-07-31 全天开发记录已导出到 .codely-cli/memory/tech-doc-2026-07-31.md。包含：转场修复、BGM播放器、构建打包、怪物卡组系统(7步完整实现)、BOM/图片路径修复、场景对象位置说明、Git提交记录。
